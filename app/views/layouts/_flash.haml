- flash.each do |key, value|
  %article{class: "alert alert-#{key} alert-dismissible", role: 'alert'}
    %button{type: 'button', class: 'close', 'data-dismiss' => 'alert', 'aria-label' => 'Close'}
      %span{'aria-hidden' => 'true'} &times;
    = value
